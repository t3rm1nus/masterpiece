{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node", "config": { "includeFiles": ["dist/client/**"] } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ],
  "outputDirectory": "dist/client"
}
